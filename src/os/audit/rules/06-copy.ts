import { type Regla } from "./tipos";

/**
 * 06 · MENSAJE Y COPY
 *
 * Señales disponibles:
 *   copy.extension  number · palabras de texto en la home
 *   seo.descripcion, seo.h1 · el mensaje que declara la página
 *
 * Sin reglas todavía. Añade las tuyas a este array: no hace falta tocar ningún
 * otro fichero. Lee `docs/guia-equipo.md` antes de empezar.
 */
export const REGLAS: Regla[] = [];
